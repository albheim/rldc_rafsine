# TODO
Remove logger delay
Mät uttemp pga tau1 och tau2
Implementera straff för att slänga jobb (och släng jobb istället för att köa), testa först på simple
Round robin sched or place on all?
testa automatisk normalisering
sensor strips gone, better to stay similar to simple sim?
let action be desired load dist isntead of load, and placement will be done to get as close as possible to that
handle overloading servers correctly

# Notes
server flow 1 -> 3.15, normal 0.0296
crah flow 1 -> 0.0937, normal 0.0458

# Next
Becomes slower quickly, after 16000 seconds of in simulation time it is taking 9 seconds to simulate one second and it seems to be python not rafsine
After 25000 insim secs the ratio is 1:16

# Meeting
Added cost for all jobs in queue
Sum of server flow way bigger than sum of crah flow, but that is not how it looks in visual
get_averages seems to slow down simulation, 1000 timesteps and get_averages go from taking nothing to taking 0.7 second to run

# Notes
Run alg scheduling with rl crah settings
Crah zones with placement
Baseline crah undvik fullt blås och undvik brinna upp
ambient temp and variable job load
split into rafsine_flows.py and simple_flows.py that is used by dc.py

# Next Meeting
What is the plot we want?
What data should be logged?
Have sinusoidal outdoor temp that we can measure?
Multi agent https://bair.berkeley.edu/blog/2018/12/12/rllib/

Simple med n=360 verkar droppa en hel del även om det går utan, svag trend att den lär sig.
Simple med n=10 droppar inget

# Notes, gör till tisdag
Run with less servers and compare to load_balanced
Run for every rack and compare to load_balanced
Run with max flow and compare
Plot all loads
Plot cpu temp
Plot energy decrease
Plot envelope with 95 conf interval
Skriv RL 1-1.5 pages, förklara för rickard/johannes vad vi gör (var övertydlig), reference ray and PPO
Test with smarter net (conv and stuff)

# Next Meeting
Vad är bra